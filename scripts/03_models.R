# Ajuste de GAMs y PCA

# Cargar datos auditados
audited_data <- readRDS("../data/processed/audited_data.rds")

# Guardar modelo (ejemplo básico)
# model_gam <- ... (código del modelo aquí)

# Guardar modelo
# if (!dir.exists("../data/processed")) {
#   dir.create("../data/processed", recursive = TRUE)
# }
# saveRDS(model_gam, "../data/processed/model_gam.rds")
